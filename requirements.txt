# Core FastAPI and server dependencies
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.5.0

# HTTP requests
aiohttp>=3.9.1

# Image processing (station logos, QR codes)
Pillow>=10.1.0

# QR code generation
qrcode[pil]>=7.4.2

# Chromecast support
PyChromecast>=13.1.0

# Sendspin protocol client — used as an ARTWORK/METADATA display client to
# receive Music Assistant album art over the LAN (binary frames, no internet).
# Not pinned: it must keep up with Music Assistant. 9.x is the first version
# with encryption and PIN pairing, which the client code now uses.
aiosendspin>=9.1.1

# Configuration
PyYAML>=6.0.1

# Development and testing dependencies
pytest>=7.4.3
pytest-asyncio>=0.21.1
httpx>=0.25.2  # For testing FastAPI endpoints
